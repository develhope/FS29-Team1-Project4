import { DATA } from "../database";

// Tutti i Progetti e Tutte le esperienze

const db = DATA;

export const allProject = db.project;
export const allExperience = db.someExperience;
export const allProgram = db.program;
